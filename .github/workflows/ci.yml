name: CI

on:
  pull_request:
  push:
    # `prerelease` is this fork's default branch and the one work merges into,
    # so it needs CI on push as much as `main` does. Without it a PR merge
    # lands on the shipping branch with nothing re-run against the merge
    # result. `main` stays listed to keep this file upstream-shaped.
    branches: [main, prerelease]

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
      - run: npm ci
      - run: npm run typecheck
      # test:coverage, not test: it runs the same suite and additionally
      # enforces the coverage ratchet in vitest.config.ts. Plain `npm run test`
      # passes on a change that drops coverage, which is what the ratchet is
      # there to stop.
      - run: npm run test:coverage
      - run: npm run test:mutations
      - run: npm run build
